таблица:

bets:

id int  -номер id
date string   -время начало матча
game enum ('Игра', 'Лига', 'Групповой этап')   -   перечисление будет 3 варианта: игра, лига, групповой этап (предварительно)  -по умолчанию ('Игра')
team1  string   -   название команды 1(кстати команды можно в отдельную таблтцу добавить можно и потом выбрать их-но на пока так! )
team2  string    -   название команды 2
emblem_t1 string    -эмблема/знак команды  1
emblem_t2  string   -эмблема/знак команды  2
sum_t1 int  -сумма поставленные на команду 1
sum_t2 int  -сумма поставленные на команду 2
coef_t1 int -коеффицент команды 1
coef_t2 int -коеффицент команды 2
percent_t1 int  -процент команды 1
percent_t2 int  -процент команды 2
result enum ('Счет', 'Ожидание', 'Отменен', 'Перенесен')  -результат , есть 3 варианта: счет, статус ожидание, матч отменен (предварительно) -по умолчанию ('Ожидание')
created_at string  -время создание таблицы
updated_at string  -время обновление таблицы


count_t1-счет команды 1
count_t2-счет команды 2
status-enum ('Ожидание', 'Закончен', 'Live')  статус матча	  -по умолчанию ('Ожидание')
