

РЕАЛИЗОВАНЫ:



В ЭТОМ ЖЕ ДИРЕКТОРИИ ЕСТЬ ФОТО "Stavki.png" -- СХЕМА ТАБЛИЦ... ВСЕ ОНИ СОЗДАНЫ, В ТОМ ЧИСЛЕ ИХ ОТНОШЕНИЯ И СВЯЗЫ(главная часть). 








1) Основные настройки пользователя:
        юзеры:
            1. админ
            2. юзер
            3. модератор
          
     

    
3) На главной странице личного кабинета пользователя: 
        разделены на 
            1. текущие
            2. предстоящие
            3. прошедшие матчи 
            4. их вывод ,
    

4) Административный раздел
        юзеры: 
            1. изменение
            2. удаление
        матчи: 
            1. добавление
            2. удаление
            3. поиск:
                1. по матчам
                2. предстоящим
                3. текущим
                4. прошедшим
                5. количествам









НАДО РЕАЛИЗОВАТЬ:  (дэдлайн)


1) На главной странице личного кабинета пользователя: 
        код: реализовать "ставки"
            1. изменение коэффицента команды в зависимости от ставок(код)
            2. показывать процент (код) 
            3. динамичаски вывод "потенциальный выигрыш"
            4. их сохранение бд таблицы юзера/клиента
            5. после выигрыша перечислить деньги на счет юзера/клиента
            6. после выигрыша брать коммисию и их перечислить на счет админа...


2) Административный раздел
        (матчи: добавление,удаление, поиск по матчам, предстоящим, текущим, прошедшим, количаствам (надо внести некоторые изменения))
        код:
            1. одобрять вывод средств юзера
            2. комисионные(изменения для юзеров)


3)Модуль «Мои ставки»
        (таблицы реализованы)
        код: динамически выводить данные(Баланс, Всего выводов, Всего пополнений, Компенсаций, Признак, что баланс сходится, Заработок на «Название игры») в этом разделе    

4) Модуль «Баланс»
        (таблицы реализованы)
        пополнение баланса  
        вывод средств
        история пополнения/выводов
        коммисия от выигрыша(в разделе админа показываем)

5) Модуль "Техническая поддержка" 
        реализовать и код и таблицу

6) Модуль «Часто задаваемые вопросы»
        реализовать и код и таблицу

7) Модуль Новости/Блог
        реализовать и код и таблицу

8) Модуль "Чат для пользователей"
        реализовать и код и таблицу


9) Основные настройки пользователя:
        отправить оповещения: да или нет
